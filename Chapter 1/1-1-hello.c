#include <stdio.h>

/*This code came from the book.*/
/*Tinker around with it to see the various ways the compiler will react to
  the changes you made. Make an effort to read the messages the compiler tells 
  you*/
/*Try compiling the file with different flags too see what the different stages
  of compilation look like.*/
main()
{
	printf("hello, world");
	
}
